apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "affiliateleads.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "affiliateleads.name" . }}
    helm.sh/chart: {{ include "affiliateleads.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "affiliateleads.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
